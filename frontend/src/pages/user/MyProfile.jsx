import { useMyAccount } from "../../hooks/useMyAccount";
import { shortDate } from "../../utils/format";
export default function MyProfile(){const {loading,error,profile}=useMyAccount();if(loading)return <div className="bank-surface p-8 text-slate-500">Loading profile…</div>;if(error)return <div className="rounded-xl border border-red-200 bg-red-50 p-4 text-red-700">{error}</div>;const initials=(profile?.fullName||"U").split(" ").map(x=>x[0]).slice(0,2).join("").toUpperCase();return <div className="max-w-3xl bank-surface overflow-hidden"><div className="bg-gradient-to-r from-slate-900 via-blue-900 to-teal-800 h-32"/><div className="px-6 sm:px-8 pb-8"><div className="-mt-12 h-24 w-24 rounded-3xl border-4 border-white bg-blue-600 text-white flex items-center justify-center text-2xl font-extrabold shadow-lg">{initials}</div><h2 className="mt-4 text-2xl font-extrabold">{profile?.fullName}</h2><p className="text-slate-500 mt-1">{profile?.email}</p><div className="mt-7 grid sm:grid-cols-2 gap-4">{[["Phone number",profile?.phoneNumber],["Member since",shortDate(profile?.createdAt)],["Customer role",profile?.role],["Account status",profile?.active?"Active":"Inactive"]].map(([l,v])=><div key={l} className="rounded-2xl bg-slate-50 border border-slate-100 p-4"><p className="text-xs uppercase tracking-wider font-bold text-slate-400">{l}</p><p className="mt-2 font-semibold text-slate-800">{v||"—"}</p></div>)}</div></div></div>}
